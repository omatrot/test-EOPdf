export class ReportRun {

  public categoryId: number;
  public modelId: number;

  constructor(categoryId: number, modelId: number) {
    this.categoryId = categoryId;
    this.modelId = modelId;
  }

}
